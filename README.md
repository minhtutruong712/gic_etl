# etl

1. For this DWH project, we use remote server for ETL process (in an attempt to build ODS at the beginning)
2. In the need of centralizing ETL scripts, this repo is created.

For data security:
- Access to the remote server is restricted to authorized team members.
- This repo contains only transformation logic — no raw data or sample outputs are committed.
