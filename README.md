# gic_etl
This repo is meant to be temporary 
1. For GIC DWH project, we use remote server for ETL process (in an attempt to build ODS for GIC at the beginning)
2. In the need of centralizing ETL scripts, this repo is created.
